# Netlify configuration file
# Docs: https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  command = "npm run build"
  publish = "dist"

# Optional: Redirects and headers can be added below
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
